secrets:
  server: "https://homeserver" # The bot's homeserver
  username: "ai" 
  password: "" 
  logroom: "!someroom:server" # If needed
  admins: # Admin list of users if needed
    - "someuser@example.mil"
keys:
  youtube_api: "" 
  wigle: "" # Should be base64 encoded basic auth. echo user:password | base64 
  twilio: "" # Not base64 but simple basic auth, "user:password" 
  virus_total: ""
  shodan: "" 
  sectrails: "" 
config:
  domain: "http://localhost" # Configure your fqdn or ip here
  output_dir: "/var/www/html/" # Use this if you want to set up 